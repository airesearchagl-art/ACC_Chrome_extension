# 📖 ACC 指摘事項ビューア 導入・操作マニュアル

このマニュアルでは、拡張機能のセットアップから日常の操作方法までを、初心者の方にもわかりやすく解説します。

---

## 🛠 準備するもの

- **Google Chrome** ブラウザ（バージョン 116 以上）
- **Autodesk アカウント**（対象プロジェクトの指摘事項を閲覧できる権限が必要です）
- **Client ID**（管理者から共有されるか、後述の手順で自分で作成します）

---

## 1. インストール手順

まだ社内配布されていない場合は、以下の手順であなたの Chrome に追加します。

1. **ファイルをダウンロード**: 開発チームから提供されたフォルダ（`ACC_Chrome_extension`）をデスクトップなどに保存します。
2. **拡張機能画面を開く**: Chrome の右上「︙」メニュー ＞ **拡張機能** ＞ **拡張機能を管理** を開きます。
3. **開発者モードをオン**: 画面右上の「デベロッパー モード」スイッチを **オン** にします。
4. **読み込み**: **パッケージ化されていない拡張機能を読み込む** ボタンを押し、保存したフォルダ（`ACC_Chrome_extension`）を選択します。
5. **拡張機能 ID を控える**: 追加後に表示される `ID: xxxxxxx...` という英数字文字列をコピーしておきます（次のステップで使います）。

---

## 2. 初期設定（APS アプリの登録）

> ✅ 既に設定済みのパッケージを受け取っている場合は、このステップは不要です。

拡張機能が Autodesk のデータに安全にアクセスするための「鍵（Client ID）」を作成します。

1. **APS ポータルにログイン**: [APS Developer Portal](https://aps.autodesk.com/myapps/) にアクセスします。
2. **アプリ作成**: **Create Application** をクリックし、任意の名前でアプリを作成します。
3. **Callback URL の設定** ← ここが重要です！
   - 手順 1-5 でコピーした拡張機能 ID を使い、以下の形式で URL を登録します:
     ```
     https://<拡張機能ID>.chromiumapp.org/
     ```
   - 例: `https://abcdefghijklmnopabcdefghijklmnop.chromiumapp.org/`
4. **Client ID を貼り付け**: 発行された **Client ID** を、フォルダ内の `src/utils/config.js` の以下の箇所に貼り付けて保存します（**Client Secret は不要**です）:
   ```js
   CLIENT_ID: 'ここに Client ID を貼り付ける',
   ```

> 🔒 **セキュリティについて**: この拡張機能は OAuth 2.0 PKCE 方式を採用しており、Client Secret をアプリ内に持ちません。認証情報はブラウザを閉じると自動的に消去されます。

---

## 3. 基本的な使い方

### ① サイドパネルを開く

> ⚠️ この拡張機能のサイドパネルは、**Autodesk のページ（autodesk.com）を開いているときのみ** 起動できます。

1. まず、ブラウザで **ACC（Autodesk Construction Cloud）のウェブサイト** を開きます。
2. Chrome ツールバー右上の拡張機能アイコン（🔧）をクリックすると、**ブラウザ右側にサイドパネルが展開**されます。
3. サイドパネルは ACC のページを操作しながら横に表示されるため、行き来する手間がありません。

### ② ログインとプロジェクト選択

1. サイドパネルの **[Autodesk でログイン]** ボタンを押します。
2. 開いた画面でいつもの Autodesk ID／パスワードでサインインします。
3. **ハブ（会社名）** ＞ **プロジェクト名** の順にドロップダウンで選ぶと、指摘事項が一覧で表示されます。

### ③ 指摘事項を確認する

| 見方 | 説明 |
|---|---|
| **色バッジ** | 「未対応」は赤、「完了」は緑など、ステータスを直感的に識別できます |
| **📎N バッジ** | 添付ファイルがある指摘は、件数がアイコン付きで表示されます |
| **赤い日付** | 期限が過ぎている指摘は、期限日が赤字で強調されます |
| **検索バー** | 担当者名やタイトルを入力すると、入力と同時に一覧が絞り込まれます。`Esc` キーまたは ✕ ボタンでリセットできます |
| **タイトルをクリック** | 詳細情報（説明・担当者・作成日など）を確認できます |

### ④ コメントを返す（クイック返信）

1. 各行の右端にある 💬 ボタンを押すと、その場で返信パネルが開きます。
2. 過去のやり取りがパネル内に表示されます。
3. 下の入力欄に返信を書き、**[送信]** ボタン（または `Ctrl + Enter`）を押せば完了です。
4. パネルを閉じなくても続けてコメントを投稿できます。

**定型文ボタン**: よく使う返信はワンクリックで自動入力・即送信できます。

| ボタン | 送信されるテキスト |
|---|---|
| 確認済 | 確認済みです。 |
| 是正完了 | 是正が完了しました。 |
| 次回定例で | 次回定例にて確認します。 |

### ⑤ 複数の指摘を一括で「回答済」にする

大量の指摘を一度に処理したい場合に便利です。

1. 各行の左端の **チェックボックス** にチェックを入れます（ヘッダー行のチェックで全選択も可能です）。
2. 画面下部に **一括操作バー** が表示されます。
3. **[✓ 回答済にする]** ボタンを押すと、選択した全指摘のステータスが一括で変更されます。
4. 完了後、自動的に一覧が更新されます。

> ⚠️ 一度に操作できるのは最大 100 件です。

---

## 4. ステータスバッジ一覧

| バッジ表示 | 意味 |
|---|---|
| 🔴 未対応 | まだ誰も手をつけていない |
| 🟣 保留中 | 対応を保留している |
| 🟠 対応中 | 現在対応中 |
| 🟢 作業完了 | 作業は完了したが承認待ち |
| 🔵 回答済 | 回答・確認が完了した |
| 🔴 否認 | 承認されなかった |
| 🟢 完了 | 全て完了・クローズ |
| ⚫ 無効 | 対象外として無効化された |
| ⚪ 下書き | まだ公開されていない下書き |

---

## 5. よくある質問（FAQ）

**Q. アイコンをクリックしてもサイドパネルが開きません。**
A. この拡張機能は **autodesk.com のページを表示中のタブでのみ** 起動します。まず ACC（`acc.autodesk.com` 等）のページを開いてから、もう一度アイコンをクリックしてください。

**Q. ログインボタンを押しても認証画面が開きません。**
A. ブラウザのポップアップブロック機能が働いている可能性があります。アドレスバー右端に表示されるブロック通知を確認し、ポップアップを「許可」してください。

**Q. 自分のプロジェクトが一覧に表示されません。**
A. ACC のウェブサイトで、該当プロジェクトの「指摘事項（Issues）」を閲覧できる権限があるか確認してください。権限がない場合は、プロジェクト管理者に付与を依頼してください。

**Q. ログアウトしたいときは？**
A. サイドパネル右上の **[ログアウト]** ボタンを押してください。なお、ブラウザを完全に終了すると、セキュリティのため認証情報は自動的に削除され、次回起動時には再度ログインが必要になります。

**Q. コメントを誤送信してしまいました。**
A. 現時点では拡張機能からの削除機能はありません。ACC のウェブサイトから直接コメントを編集・削除してください。

**Q. 複数のプロジェクトを確認したいです。**
A. ハブ・プロジェクトのドロップダウンからいつでも切り替えられます。別のプロジェクトを選ぶと、自動的に一覧が更新されます。
